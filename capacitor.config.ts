import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'qc-hybrid-app',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
